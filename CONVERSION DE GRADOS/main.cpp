/*COVERSION DE GRADOS*/

#include <iostream> // usamos espacio de nombre estandar cout, salida, cin
//esntrada, cerr, salida de errores, clog se usa para mensaje de logical_and

int main() {
    float celsius, fahrenheint;

    cout << "Grados Celsius: ";
    cin >> Celsius;

    fahrenheint = (celsius * 9 / 5) + 32;

    cout << celsius << "°C son " << fahrenheit << "°F" <<endl;

    return 0;
}
